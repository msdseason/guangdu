export function availableSlots(){const result:string[]=[];const today=new Date().toLocaleDateString('en-CA',{timeZone:'Asia/Shanghai'});const base=new Date(today+'T12:00:00+08:00');for(let i=1;i<=14;i++){const d=new Date(base.getTime()+i*86400000);if(d.getUTCDay()===0||d.getUTCDay()===1)continue;const day=d.toLocaleDateString('en-CA',{timeZone:'Asia/Shanghai'});for(const t of ['10:00','14:00','16:00','19:00'])result.push(day+'T'+t);}return result;}
